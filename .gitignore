# Jekyll
_site/
.dist/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby
Gemfile.lock
vendor/
.bundle/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~

# Внутренняя документация и концепты — только локально, в git не уходят
docs/

# Черновики. Сейчас версионируются (полезно, если работаете с разных машин).
# Раскомментируйте, если они тоже должны остаться только на этом компьютере:
# drafts/
