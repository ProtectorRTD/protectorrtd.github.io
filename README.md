# protectorrtd.github.io

Сайт на GitHub Pages: Jekyll, тема `jekyll-theme-minimal` + собственный макет.

Внутренняя документация и концепты — в папке `docs/` (в сборку не попадает).
Черновики — в `drafts/` (тоже не публикуются).
